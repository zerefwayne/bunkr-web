import {
    FETCH_COURSE,
    FETCH_COURSES
} from "./actions.type";

import {
    SET_ACTIVE_COURSE,
    SET_COURSES
} from "./mutations.type";

const state = {};
const mutations = {};
const actions = {};
const getters = {};

export default {
    state,
    mutations,
    actions,
    getters
}